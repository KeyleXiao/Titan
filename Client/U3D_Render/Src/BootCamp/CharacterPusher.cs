using System;
using UnityEngine;

[Serializable]
public class CharacterPusher : MonoBehaviour
{
    //public float pushPower = 2f;

    //public  void OnControllerColliderHit(ControllerColliderHit hit)
    //{
    //    Rigidbody attachedRigidbody = hit.collider.attachedRigidbody;
    //    if (((attachedRigidbody != null) && !attachedRigidbody.isKinematic) && (hit.moveDirection.y >= -0.3f))
    //    {
    //        Vector3 vector = new Vector3(hit.moveDirection.x, (float) 0, hit.moveDirection.z);
    //        attachedRigidbody.velocity = (Vector3) (vector * this.pushPower);
    //    }
    //}
}

